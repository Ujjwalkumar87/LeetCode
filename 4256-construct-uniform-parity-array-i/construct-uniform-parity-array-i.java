class Solution {
    public boolean uniformArray(int[] nums1) {
        // int[] nums2 = new int[nums1.length];
        // for(int i = 0; i < nums1.length; i++){
        //     if(nums2[i] = nums1[i]) return true;
        // }
        // for(int j = 0; j < nums1.length; j++){
        //     if(nums2[i] = nums1[i] - nums1[j] && j != i) return true;
        // }
        // return false;

        // chat gpt 

        
        return true;
    }
}